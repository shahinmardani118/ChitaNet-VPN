version: '3.8'

services:
  app:
    build: .
    container_name: chitanet-vpn
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PORT=5000
    volumes:
      - ./database.db:/app/database.db
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped